
const page = () => {
  return (
    <div>
      <h>This is a Private Page. It's not routable..;)</h>
      <p>Because we have addded underscore(_) before the folder name. </p>
      <p>Private folders can be created by prefixing a folder with an underscore: _folderName</p>
    </div>
  )
}

export default page
